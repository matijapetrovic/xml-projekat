import { XonomyDirective } from './xonomy.directive';

describe('XonomyDirective', () => {
  it('should create an instance', () => {
    const directive = new XonomyDirective();
    expect(directive).toBeTruthy();
  });
});
